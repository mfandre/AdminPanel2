@using AdminPanel.Helpers.Forms;
@{
    Layout = "~/Views/Shared/_LoginPage.cshtml";

    Random random = new Random();
    int randomNumber = random.Next(1, 6);

    ViewBag.BackgroundImage = "img/superbox/superbox-full-" + randomNumber + ".jpg";
}



<div class="row">
	<div class="col-xs-12 col-sm-12 col-md-5 col-lg-4">
		<div class="well no-padding">

            @{
                Html.FormAjax("formCadastroUsuario", Url.Action("DoLogin", "Login"), AdminPanel.Helpers.Forms.FormMethod.POST,"Login")
                    .AddFormItem(new InputTextWithLabel("inputEmail", "username", "Login", "Preencha com seu login", true))
                    .AddFormItem(new InputPasswordWithLabel("inputLogin", "password", "Senha", "Preencha com sua senha",true))
                    .Render();
            }


		@*	<form action="index.html" id="login-form" class="smart-form client-form">
				<header>
					Login
				</header>
               
				<fieldset>
									
					<section>
						<label class="label">E-mail</label>
						<label class="input"> 
                            <i class="icon-append fa fa-user"></i>
							<input type="email" name="email">
							<b class="tooltip tooltip-top-right"><i class="fa fa-user txt-color-teal"></i> Please enter email address/username</b>
						</label>
					</section>

					<section>
						<label class="label">Password</label>
						<label class="input"> <i class="icon-append fa fa-lock"></i>
							<input type="password" name="password">
							<b class="tooltip tooltip-top-right"><i class="fa fa-lock txt-color-teal"></i> Enter your password</b> </label>
					</section>

					<section>
						<label class="checkbox">
							<input type="checkbox" name="remember" checked="">
							<i></i>Stay signed in</label>
					</section>
				</fieldset>
				<footer>
					<button type="submit" class="btn btn-primary">
						Sign in
					</button>
				</footer>
			</form>

            <script type="text/javascript">
                $(function () {
                    // Validation
                    $("#login-form").validate({
                        // Rules for form validation
                        rules: {
                            email: {
                                required: true,
                                email: true
                            },
                            password: {
                                required: true,
                                minlength: 3,
                                maxlength: 20
                            }
                        },

                        // Messages for form validation
                        messages: {
                            email: {
                                required: 'Please enter your email address',
                                email: 'Please enter a VALID email address'
                            },
                            password: {
                                required: 'Please enter your password'
                            }
                        },

                        // Do not change code below
                        errorPlacement: function (error, element) {
                            error.insertAfter(element.parent());
                        }
                    });
                });
		</script>*@


		</div>
	</div>
</div>